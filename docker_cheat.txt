docker run -d -p 8000:8000 -v /path/to/local/model_db:/model_db --name test_1 ml_ops_showcase 
docker run -d -p 8000:8000 ml_ops_finance_proj
docker build -t ml_ops_showcase .

# for minimal requirements.txt
pip install pipreqs
than navitage to folder and run: pipreqs